terms = {'var':'A variable','if':'Represent a boolean expression check','for':'Loop through a range or a container.','index':'You can access element by using this.',
            'List':'A group of elements.',
            'set':'Collections of unique items',
            'dictionay':'a group of key-value pairs.',
            'sort()':'Change the list permanently.',
            'sorted()':'Return a sorted list but dont affect the original one.'
        }

for key in terms:
    print(f"{key} : {terms[key]}\n")

